# Windows-only requirements for ghostmic
# This file delegates to the canonical project-level requirements_windows.txt
# Use pip -r from the ghostmic directory to include the top-level file.
-r ../requirements_windows.txt
