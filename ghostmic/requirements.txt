# This file delegates to the canonical project-level requirements.txt.
# Keep a single source of truth at the repository root. Use the top-level
# requirements.txt to manage dependency versions. This helper allows
# installing from the `ghostmic` directory with the same dependency set.
-r ../requirements.txt
